class LogStreamTest {

	static function main() {
		Console.log('1 (log)');
		Console.success('2 (success)');
		Console.warn('3 (warn)');
		Console.error('4 (error)');
		Console.warn('5 (warn)');
		Console.log('6 (log)');
	}

}